NOTE:
Regarding the header and cpp files (which come in pairs), #include things necessary for the declaration in the header, and include everything else (necessary for the definition) in the cpp. Also include the corresponding header in its cpp pair too

enemy monsters will have TreasureClass (item drops)
friendly mosters will have basic equipment (sword + helmet)



what about a pokemon style/themed roguelike?

texture:
the Monster class will load 2 textures: battle front, and battle back.
[scrap that]
the Monster class will be an 'abstract' class? So no objects are directly created of that class
it will have 2 babby(sub) classes:
    playerMonster
    wildMonster
playerMonster loads the battleback sprite
wildMonster loads the battlefront sprite



MonsterHandler class
MH takes in a renderer during its construction
MH is responsible for creating and returning references to Monsters
PlayerMonster JMon = MonsterHandler.createPlayerMonster(squirtle, 100);



GUI:
UserInterface class (should this be called "scene"? "SceneHandler"?)
Keeps track of which scene is currently loaded? (only one: battle, for now)
unloads / switches scenes (battle UI, items menu, overworld, etc)
creates and holds references(?) to viewports
when an image wants to render, it .... TODO?

create the window/renderer
create the playerMonster/wildMonster
call SceneHandler.createBattleScene(renderer, playerMonster, wildMonster)
battle/turn logic
SceneHandler is responsible for rendering to screen?



Scene class:




Battle:
while (!win condition) [both mons are alive, no one's escaped]:
    check who's turn it is
    if player turn:
        handle player actions?
        check for battle win conditions (either mon's hp <=0)
        if win condition = true:
            handle win (battle end)
        set turn flag to enemy's turn
        continue statement?
    else (if enemy turn):
        handle enemy actions?
        check for battle win conditions
        if win condition = true:
            handle player loss
        set turn flag back to player's turn
end?
